// postcss.config.js (ESM)
export default {
  plugins: {
    '@tailwindcss/postcss': {},   // Tailwind v4 PostCSS plugin
    // autoprefixer: {},          // optional (install if you enable it)
  },
};